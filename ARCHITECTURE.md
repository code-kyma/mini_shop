# 📦 Архитектура MiniShop после рефакторинга

## 🎯 Обзор проекта

MiniShop - это современное веб-приложение для электронной коммерции, построенное на Vue 3 + Vite. 

**До рефакторинга:** Весь код был в одном файле `App.vue` (990 строк)  
**После рефакторинга:** Код разделен на компоненты и composables согласно принципам Single Responsibility Principle (SRP)

---

## 📂 Структура проекта

```
src/
├── App.vue                           # Главный компонент (оркестратор)
├── main.js                           # Точка входа
│
├── components/                       # UI компоненты
│   ├── AppHeader.vue                # ✨ Шапка с логотипом и поиском
│   ├── FilterSidebar.vue            # ✨ Фильтры (категории, цена, сортировка)
│   ├── ProductsGrid.vue             # ✨ Сетка товаров
│   ├── CartSidebar.vue              # ✨ Боковая панель корзины
│   ├── ProductModal.vue             # ✨ Модальное окно товара
│   ├── NotificationToast.vue        # ✨ Система уведомлений
│   └── ProductCard.vue              # Карточка товара (существовал ранее)
│
└── composables/                      # Логика приложения (composition functions)
    ├── useProducts.js               # ✨ Логика товаров и фильтрации
    ├── useCart.js                   # ✨ Логика корзины
    └── useNotification.js           # ✨ Логика уведомлений
```

---

## 🧩 Описание компонентов

### 1️⃣ **AppHeader.vue** - Навигационная шапка

**Назначение:** Отображение логотипа, поисковой строки и иконки корзины

**Props:**
- `searchQuery: String` - Текущий поисковый запрос
- `cartItems: Array` - Список товаров в корзине

**Events:**
- `@update:search-query` - Обновление поискового запроса
- `@filter` - Триггер поиска
- `@toggle-cart` - Открыть/закрыть корзину

**Особенности:**
- Адаптивная шапка
- Отображение количества товаров в корзине (badge)
- Поиск при нажатии Enter или кнопки Search

---

### 2️⃣ **FilterSidebar.vue** - Панель фильтров

**Назначение:** Фильтрация и сортировка товаров

**Props:**
- `categories: Array` - Список категорий
- `modelValue: String` - Выбранная категория (v-model)
- `priceRange: Array` - Диапазон цен [min, max]
- `sortBy: String` - Тип сортировки

**Events:**
- `@update:modelValue` - Изменение категории
- `@update:price-range` - Изменение ценового диапазона
- `@update:sort-by` - Изменение сортировки

**Фильтры:**
- ✅ По категориям (All Products, Electronics, Fashion, etc.)
- ✅ По цене (два слайдера: min и max)
- ✅ Сортировка (популярность, цена низкая-высокая, новизна)

---

### 3️⃣ **ProductsGrid.vue** - Сетка товаров

**Назначение:** Отображение отфильтрованных товаров

**Props:**
- `filteredProducts: Array` - Отфильтрованные товары
- `selectedCategory: String` - Текущая категория

**Events:**
- `@add-to-cart` - Добавить товар в корзину
- `@view-details` - Открыть детали товара
- `@reset-filters` - Сбросить все фильтры

**Особенности:**
- Сетка с адаптивным количеством колонок
- Отображение "товаров не найдено"
- Интеграция с компонентом ProductCard

---

### 4️⃣ **CartSidebar.vue** - Боковая панель корзины

**Назначение:** Управление содержимым корзины

**Props:**
- `cartItems: Array` - Список товаров в корзине
- `showCart: Boolean` - Видимость корзины
- `cartTotal: Number` - Итоговая сумма

**Events:**
- `@toggle-cart` - Закрыть корзину
- `@increase-quantity` - Увеличить количество товара
- `@decrease-quantity` - Уменьшить количество товара
- `@remove-from-cart` - Удалить товар
- `@checkout` - Оформить заказ
- `@clear-cart` - Очистить корзину

**Особенности:**
- Анимация появления (slideIn)
- Фиксированное положение справа
- Отображение пустой корзины
- Кнопки для операций с корзиной

---

### 5️⃣ **ProductModal.vue** - Модальное окно товара

**Назначение:** Просмотр полной информации о товаре

**Props:**
- `selectedProduct: Object` - Данные выбранного товара

**Events:**
- `@close` - Закрыть модальное окно
- `@add-to-cart` - Добавить в корзину (из модального окна)

**Информация о товаре:**
- 🖼️ Изображение (эмодзи с градиентом)
- 📝 Название и категория
- 💰 Цена
- 📖 Описание
- ⭐ Рейтинг

---

### 6️⃣ **NotificationToast.vue** - Система уведомлений

**Назначение:** Отображение уведомлений пользователю

**Props:**
- `notification: Object` - Объект уведомления { message, type }

**Типы уведомлений:**
- 🟢 `success` - Успешные операции (зеленый)
- 🔵 `info` - Информационные сообщения (синий)

**Особенности:**
- Автоматическое исчезновение через 3 секунды
- Плавная анимация slideUp
- Фиксированная позиция внизу справа

---

### 7️⃣ **ProductCard.vue** - Карточка товара

**Назначение:** Отображение одного товара в сетке

**Была в оригинальной версии, остается без изменений**

**Props:**
- `product: Object` - Данные товара

**Events:**
- `@add-to-cart` - Добавить в корзину
- `@view-details` - Просмотреть детали

---

## 🎣 Описание Composables

### 1️⃣ **useProducts.js** - Логика товаров

**Назначение:** Управление товарами, фильтрацией и сортировкой

```javascript
const {
  products,           // ref([...]) - Массив всех товаров
  categories,         // computed - Список уникальных категорий
  searchQuery,        // ref('') - Текущий поисковый запрос
  selectedCategory,   // ref('All Products') - Выбранная категория
  priceRange,         // ref([0, 1000]) - Ценовой диапазон
  sortBy,             // ref('popular') - Тип сортировки
  filteredProducts,   // computed - Отфильтрованные товары
  resetFilters        // function() - Сброс всех фильтров
} = useProducts()
```

**Функционал:**
- ✅ 20 товаров в 5 категориях
- ✅ Фильтрация по категориям
- ✅ Поиск по названию и описанию (case-insensitive)
- ✅ Фильтрация по диапазону цен
- ✅ Сортировка:
  - Популярность (порядок по умолчанию)
  - Цена от меньшей к большей
  - Цена от большей к меньшей
  - Новизна (по ID в обратном порядке)

**Данные товара:**
```javascript
{
  id: Number,
  name: String,
  price: Number,
  category: String,
  description: String,
  rating: Number,
  emoji: String
}
```

---

### 2️⃣ **useCart.js** - Логика корзины

**Назначение:** Управление покупками

```javascript
const {
  cartItems,         // ref([]) - Товары в корзине
  showCart,          // ref(false) - Видимость корзины
  cartTotal,         // computed - Итоговая сумма
  addToCart,         // function(product) - Добавить товар
  removeFromCart,    // function(productId) - Удалить товар
  increaseQuantity,  // function(productId) - Увеличить кол-во
  decreaseQuantity,  // function(productId) - Уменьшить кол-во
  clearCart,         // function() - Очистить корзину
  checkout,          // function() - Оформить заказ
  toggleCart         // function() - Открыть/закрыть
} = useCart()
```

**Функционал:**
- ✅ Добавление товара (или увеличение количества если товар уже в корзине)
- ✅ Удаление товара из корзины
- ✅ Изменение количества товара
- ✅ Подсчет итоговой суммы (автоматический)
- ✅ Оформление заказа (очистка корзины)
- ✅ Управление видимостью боковой панели

**Структура товара в корзине:**
```javascript
{
  id: Number,
  name: String,
  price: Number,
  quantity: Number,    // Количество товара
  category: String,
  description: String,
  rating: Number,
  emoji: String
}
```

---

### 3️⃣ **useNotification.js** - Логика уведомлений

**Назначение:** Управление уведомлениями

```javascript
const {
  notification,    // ref(null) - Объект уведомления
  showNotification // function(message, type) - Показать уведомление
} = useNotification()
```

**Функционал:**
- ✅ Отображение уведомления
- ✅ Автоматическое исчезновение через 3 секунды
- ✅ Поддержка типов: 'success', 'info'

**Использование:**
```javascript
showNotification('Товар добавлен в корзину!', 'success')
showNotification('Ошибка при загрузке', 'info')
```

---

## 📊 Компонентные взаимосвязи

```
App.vue (оркестратор)
├── AppHeader
│   └── Получает: searchQuery, cartItems
│   └── Отправляет: update:search-query, filter, toggle-cart
│
├── FilterSidebar
│   └── Получает: categories, selectedCategory, priceRange, sortBy
│   └── Отправляет: update:modelValue, update:price-range, update:sort-by
│
├── ProductsGrid
│   ├── Получает: filteredProducts, selectedCategory
│   ├── Отправляет: add-to-cart, view-details, reset-filters
│   └── Содержит: ProductCard (x20)
│
├── CartSidebar
│   └── Получает: cartItems, showCart, cartTotal
│   └── Отправляет: toggle-cart, increase-quantity, decrease-quantity, 
│                   remove-from-cart, checkout, clear-cart
│
├── ProductModal
│   └── Получает: selectedProduct
│   └── Отправляет: close, add-to-cart
│
└── NotificationToast
    └── Получает: notification
    └── Отправляет: ничего (presentation-only)
```

---

## 🔄 Поток данных

### Сценарий 1: Добавление товара в корзину

```
ProductCard (кликнуть "Add")
    ↓
App.vue (@add-to-cart)
    ↓
useCart.addToCart()
    ↓
useNotification.showNotification()
    ↓
NotificationToast (отобразить "успешно")
CartSidebar (обновить список товаров)
```

### Сценарий 2: Фильтрация по категории

```
FilterSidebar (клик на категорию)
    ↓
App.vue (update:modelValue)
    ↓
useProducts.selectedCategory = 'Fashion'
    ↓
useProducts.filteredProducts (computed - пересчитается автоматически)
    ↓
ProductsGrid (отобразить новые товары)
```

### Сценарий 3: Просмотр деталей товара

```
ProductCard (клик "View")
    ↓
App.vue (@view-details)
    ↓
App.vue (selectedProduct = product)
    ↓
ProductModal (отобразить модальное окно)
    ↓
Пользователь может добавить в корзину или закрыть
```

---

## 🎨 Стили

### Палитра цветов

```
Primary Gradient: #667eea → #764ba2 (фиолетовый)
Success: #51cf66 (зеленый)
Info: #4dabf7 (синий)
Background: #f5f5f5 (светло-серый)
Surface: #ffffff (белый)
Border: #e0e0e0 (серый)
Text: #333333 (темно-серый)
Muted: #888888 (серый)
```

### Адаптивность

- 📱 Mobile: < 480px
- 📱 Tablet: < 768px
- 🖥️ Desktop: < 1024px
- 🖥️ Large: > 1024px

### Анимации

- `slideIn` - Появление корзины (300ms)
- `slideUp` - Появление уведомления (300ms)
- `fadeIn` - Появление модального окна (300ms)
- `fade-enter/leave` - Переход уведомления (300ms)

---

## 📈 Статистика

| Метрика | Значение |
|---------|----------|
| **Строк кода (до)** | 990 |
| **Строк кода (после)** | ~950 |
| **Компонентов** | 7 |
| **Composables** | 3 |
| **Товаров** | 20 |
| **Категорий** | 5 |

---

## 🚀 Преимущества новой архитектуры

### 1. **Разделение ответственности (SRP)**
Каждый компонент отвечает за одну функцию:
- AppHeader - только UI шапки
- FilterSidebar - только фильтры
- useCart - только логика корзины
- useNotification - только уведомления

### 2. **Переиспользуемость (Reusability)**
Composables можно использовать в других компонентах или проектах

### 3. **Тестируемость (Testability)**
Каждый composable легче тестировать отдельно

### 4. **Масштабируемость (Scalability)**
Легко добавить новые функции без изменения существующего кода

### 5. **Читаемость (Readability)**
Код стал более понятным благодаря логическому разделению

### 6. **Поддерживаемость (Maintainability)**
Ошибки легче находить и исправлять

### 7. **Производительность (Performance)**
Лучше оптимизируется для code splitting и ленивой загрузки

---

## 🔧 Как расширить приложение

### Добавить новый фильтр

```javascript
// 1. В useProducts.js добавить новый state:
const newFilterValue = ref('default')

// 2. Добавить логику фильтрации в filteredProducts computed

// 3. В FilterSidebar.vue добавить UI для фильтра

// 4. В App.vue пробросить props и events
```

### Добавить новый тип уведомления

```javascript
// 1. В NotificationToast.vue добавить стиль:
.notification.warning {
  background: #ffc107;
  color: white;
}

// 2. Использовать в коде:
showNotification('Осторожно!', 'warning')
```

### Добавить новый composable

```javascript
// 1. Создать src/composables/useNewFeature.js

// 2. Импортировать в App.vue:
import { useNewFeature } from './composables/useNewFeature'

// 3. Использовать:
const { feature } = useNewFeature()
```

---

## 📝 Возможные улучшения

- [ ] Добавить пагинацию товаров
- [ ] Реализовать wishlist (избранное)
- [ ] Добавить фильтр по рейтингу
- [ ] Интегрировать реальный API
- [ ] Добавить валидацию форм
- [ ] Хранить корзину в localStorage
- [ ] Добавить аутентификацию пользователя
- [ ] Реализовать checkout форму
- [ ] Добавить систему отзывов
- [ ] Внедрить поиск с автодополнением

---

## 📚 Дополнительные ресурсы

- 📖 Документация Vue 3: https://vuejs.org
- 📖 Composition API: https://vuejs.org/guide/extras/composition-api-faq.html
- 📖 Vite: https://vitejs.dev
- 📖 Разделение ответственности: https://en.wikipedia.org/wiki/Single-responsibility_principle

---

**Дата рефакторинга:** 28 января 2026  
**Версия:** 2.0
